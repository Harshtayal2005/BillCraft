Process:

# Registring the user(backend)
- Created index.js file for: Connecting to database and then starting the app
- app.js file for building the server and handling the routes(user requests)
- Routes folder for handling different routes(here user route)
- calling register user function present in controller logic, on receiving the route request of register
- handling all the logic for creating the user in the controller inside the registerUser function

# Registring the user(frontend)
- React hook form for efficiently dealing with the forms
- proxy setup for connecting frontend and backend on different ports to avoid CORS issue

# login the user(backend)
- added route for login functionality and loginUser function in user controller
- used jwt to create the token and send to the client in the form of cookie for session management

# login the user(frontend)
- same as register

# authenticate middleware for protected route added

# add-client functionaity added(backend)
# retrieve client functionaity added(backend)
# add-client frontend using react-hook-form
# remove client from frontend and backend
# get all the clients associated with user